# Stagger

An audio file tagger written in `python3`. Retrieves data from Spotify web API

## Install

Download the repo as a zip, install `spotipy` and `mutagen`, run `main.py`, profit!  
Currently only supports FLAC and OGG
